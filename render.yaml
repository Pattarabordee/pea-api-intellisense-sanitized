services:
  - type: web
    name: pea-line-webhook
    runtime: python
    plan: starter
    buildCommand: python -m compileall -q ais_etr
    startCommand: python -m ais_etr line-webhook-server --host 0.0.0.0 --port $PORT --output runtime/line_webhook_capture.jsonl --sqlite-output runtime/line_webhook_capture.sqlite
    healthCheckPath: /health
    envVars:
      - key: AIS_NOTIFICATION_MODE
        value: shadow
      - key: PRODUCTION_SEND_MODE
        value: blocked
      - key: LINE_CHANNEL_SECRET
        sync: false
      - key: LINE_ALLOWED_GROUP_IDS
        sync: false
      - key: LINE_ALLOWED_CHAT_HASHES
        sync: false
      - key: LINE_CAPTURE_MODE
        value: shadow

  - type: web
    name: pea-api-intellisense-api
    runtime: docker
    plan: starter
    dockerfilePath: ./apps/api-go/Dockerfile
    dockerContext: .
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: pea-api-intellisense-postgres
          property: connectionString
      - key: AIS_INBOUND_API_KEY
        sync: false
      - key: AIS_NOTIFICATION_MODE
        value: shadow
      - key: PRODUCTION_SEND_MODE
        value: blocked
      - key: CALLBACK_TRANSPORT
        value: dry_run
      - key: EMERGENCY_OFF
        value: "false"
      - key: RATE_LIMIT_PER_MINUTE
        value: "120"
      - key: ALLOWED_ORIGIN
        value: https://pea-api-intellisense-web.onrender.com

  - type: web
    name: pea-api-intellisense-web
    runtime: node
    plan: starter
    rootDir: apps/web-next
    buildCommand: npm ci && npm run build
    startCommand: npm run start
    envVars:
      - key: API_BASE_URL
        value: https://pea-api-intellisense-api.onrender.com
      - key: AIS_INBOUND_API_KEY
        sync: false
      - key: NEXT_PUBLIC_API_BASE_URL
        value: https://pea-api-intellisense-api.onrender.com

databases:
  - name: pea-api-intellisense-postgres
    plan: basic-256mb
