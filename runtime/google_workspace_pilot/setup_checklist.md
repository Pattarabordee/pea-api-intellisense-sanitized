# Superseded Setup Checklist

Do not create a sheet, configure a key, or deploy the legacy Google Workspace pilot.

The active foundation is the Render Go API with authenticated inbound requests, private evidence reports, `mode=shadow`, `production_send=blocked`, and dry-run callbacks. Historic Apps Script deployments require separate owner-led decommissioning; no external action is performed by this repository.
